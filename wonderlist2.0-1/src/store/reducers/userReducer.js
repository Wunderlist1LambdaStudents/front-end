const initialState = {
  isFetchingUser: false,
  user: {},
  userError: '',
};

export const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
